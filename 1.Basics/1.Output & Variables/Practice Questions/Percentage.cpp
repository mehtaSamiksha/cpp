#include <iostream>
using namespace std;
int main()
{
    float a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e;
    f = (a + b + c + d + e) / 5;
    cout << "Percentage of 5 subjects is : " << f;
}